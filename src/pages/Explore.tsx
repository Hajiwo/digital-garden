import { useMemo, useState } from 'react'
import { Search as SearchIcon } from 'lucide-react'
import { useSearchParams } from 'react-router-dom'
import ArticleCard from '../components/ArticleCard'
import { articles, categories } from '../data'
export default function Explore() { const [parameters] = useSearchParams(); const [query, setQuery] = useState(''), [category, setCategory] = useState(parameters.get('category') || 'All'), [sort, setSort] = useState('newest'); const list = useMemo(() => articles.filter((article) => (category === 'All' || article.tags.includes(category)) && (`${article.title} ${article.description} ${article.tags.join(' ')} ${article.contentHtml}`).toLowerCase().includes(query.toLowerCase())).sort((a, b) => sort === 'title' ? a.title.localeCompare(b.title) : sort === 'oldest' ? a.date.localeCompare(b.date) : b.date.localeCompare(a.date)), [query, category, sort]); return <section className="page explore"><p className="eyebrow">THE LIBRARY</p><h1>Explore every idea.</h1><p className="intro">Search the collection or browse dynamically by tag.</p><div className="controls"><label><SearchIcon /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search articles, tags, ideas…" /></label><select value={sort} onChange={(event) => setSort(event.target.value)}><option value="newest">Newest first</option><option value="oldest">Oldest first</option><option value="title">By title</option></select></div><div className="pills" aria-label="Filter by tag">{['All', ...categories].map((name) => <button className={category === name ? 'active' : ''} onClick={() => setCategory(name)} key={name}>{name === 'All' ? name : `#${name}`}</button>)}</div><p className="result-count">{list.length} {list.length === 1 ? 'story' : 'stories'}</p><div className="grid">{list.map((article) => <ArticleCard key={article.slug} article={article} />)}</div>{!list.length && <div className="empty">No stories found. Try a broader search.</div>}</section> }
