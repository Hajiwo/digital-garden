import{Link}from'react-router-dom';import{articles}from'../data';export default function Timeline(){const years=[...new Set(articles.map(a=>a.date.slice(0,4)))];return <section className="page timeline"><p className="eyebrow">CHRONOLOGY</p><h1>Ideas through time.</h1><p className="intro">The collection, arranged in the order it grew.</p>{years.map(y=><div className="year" key={y}><h2>{y}</h2><div>{articles.filter(a=>a.date.startsWith(y)).map(a=><Link to={'/articles/'+a.slug} key={a.slug}><time>{new Date(a.date).toLocaleDateString('en',{month:'short',day:'2-digit'})}</time><span><b>{a.title}</b><small>{a.category} · {a.readingTime} min</small></span><i>→</i></Link>)}</div></div>)}</section>}
