import { useEffect, useState } from 'react'
import { Menu, Moon, Palette, Sun, X } from 'lucide-react'
import { Link, NavLink } from 'react-router-dom'
export type WebsiteStyle = 'editorial' | 'reading' | 'geek' | 'minimal'
const styleLabels: Record<WebsiteStyle, string> = { editorial: 'Editorial', reading: 'Reading', geek: 'Geek', minimal: 'Minimal' }
export default function Header({ theme, setTheme, websiteStyle, setWebsiteStyle }: { theme: string; setTheme: (theme: string) => void; websiteStyle: WebsiteStyle; setWebsiteStyle: (style: WebsiteStyle) => void }) { const [scrolled, setScrolled] = useState(false), [open, setOpen] = useState(false); useEffect(() => { const update = () => setScrolled(scrollY > 24); addEventListener('scroll', update, { passive: true }); return () => removeEventListener('scroll', update) }, []); const links = [['/', 'Home'], ['/explore', 'Explore'], ['/timeline', 'Timeline'], ['/about', 'About'], ...(import.meta.env.DEV ? [['/developer', 'Developer']] : [])]; return <header className={scrolled ? 'header scrolled' : 'header'}><Link to="/" className="brand">C.</Link><nav className={open ? 'nav open' : 'nav'}>{links.map(([to, name]) => <NavLink key={to} to={to} onClick={() => setOpen(false)}>{name}</NavLink>)}</nav><div className="header-actions"><label className="style-switch" title="Website style"><Palette /><span className="sr-only">Website style</span><select value={websiteStyle} onChange={(event) => setWebsiteStyle(event.target.value as WebsiteStyle)}>{Object.entries(styleLabels).map(([value, label]) => <option value={value} key={value}>{label}</option>)}</select></label><button className="icon-btn" onClick={() => setTheme(theme === 'dark' ? 'light' : 'dark')} aria-label="Toggle color theme">{theme === 'dark' ? <Sun /> : <Moon />}</button><button className="icon-btn menu" onClick={() => setOpen(!open)} aria-label="Toggle menu">{open ? <X /> : <Menu />}</button></div></header> }
